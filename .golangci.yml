run:
  timeout: 30m
  modules-download-mode: readonly
  tests: false

linters:
  enable:
    - errcheck
    - ineffassign
    - staticcheck
    - unused
    - gosimple
    - govet
    - revive
    - gofmt
    - misspell
    - nakedret
    - prealloc
    - unconvert
  disable:
    - depguard

issues:
  exclude-use-default: true
  max-issues-per-linter: 50
  max-same-issues: 5
